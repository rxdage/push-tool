# 迁移到另一台机器（常开台式机）

把这套工具搬到一台常开的台式机跑,笔记本就能关。要带 **3 样东西**:

| 要带 | 怎么带 |
|---|---|
| 代码 | git 远程 clone（下面 A 步） |
| `.env`（含密钥/webhook） | **手动拷贝**（git 不含它，故意的，防泄露） |
| `backups/pushtool_backup.sql`（数据库备份 ~8MB） | **手动拷贝**（git 不含它） |

> bge-m3 模型不用带，台式机首次会自动下载(~2GB)。

---

## A. 把代码推到 git 远程（在笔记本上做一次）

1. 在 **Gitee**（国内快）或 **GitHub** 建一个**私有**空仓库，**不要**勾选"初始化 README"。
2. 复制它的 HTTPS 地址，例如 `https://gitee.com/你的名/push-tool.git`
3. 在笔记本项目目录里执行：
   ```powershell
   git remote add origin <你的仓库地址>
   git push -u origin main
   ```
   首次会要你登录：GitHub 用 Personal Access Token（不是密码），Gitee 用密码或私人令牌。

以后两台机器都能 `git pull` / `git push` 同步代码。

---

## B. 台式机首次安装

1. 装 **Docker Desktop**（首次需启用 WSL2 + 重启，按提示走）。
2. clone 代码：
   ```powershell
   git clone <你的仓库地址> push-tool
   cd push-tool
   ```
3. 把笔记本上的 **`.env`** 拷进 `push-tool` 根目录。
4. 把 **`backups/pushtool_backup.sql`** 拷进 `push-tool\backups\`（新建该文件夹）。
5. 起数据库并**恢复备份**（含表结构+数据+迁移版本，所以**不用**再跑 alembic/seed）：
   ```powershell
   docker compose up -d --wait db
   docker compose cp backups\pushtool_backup.sql db:/tmp/restore.sql
   docker compose exec -T db psql -U push -d pushtool -f /tmp/restore.sql
   ```
6. 起服务：
   ```powershell
   docker compose up -d app scheduler
   ```
7. 浏览器开 `http://localhost:8000/` 确认能看到历史 digest。

> 若不迁数据、想从零开始：跳过第 4–5 步的恢复，改用
> `docker compose run --rm app alembic upgrade head` + `docker compose run --rm app python -m app.seed.load`。

---

## C. ⚠️ 笔记本上必须停掉（否则重复推送）

搬好、台式机验证 OK 后，在**笔记本**上执行：
```powershell
docker compose down
```
**同一时间只能一台机器跑 scheduler**，否则两台都会往飞书推，变成重复消息。

---

## 以后

- 改代码：在任一台 `git pull` 同步；`.env` 和数据库各机自管，不走 git。
- 想在台式机上继续让 Claude Code 帮忙：装 Claude Code、`git clone` 后打开该文件夹即可（项目记忆是新环境，但代码/历史都在）。
- 问答 bot（若启用）：长连接是出站连接，台式机同样不需要公网/备案。
- 备份数据：随时 `docker compose exec -T db pg_dump -U push -d pushtool -f /tmp/b.sql && docker compose cp db:/tmp/b.sql backups\b.sql`。
